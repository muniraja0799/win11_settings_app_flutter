import 'package:flutter/material.dart';

class NetworkAndInternet extends StatelessWidget {
  const NetworkAndInternet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Center(
        child: Text(
          'Netwotk And Internet',
          style: TextStyle(color: Colors.white, fontSize: 50.0),
        ),
      ),
    );
  }
}
